/// console_print(string text)
// prints the given text to the console

var to_be_added = "[" + date_datetime_string(date_current_datetime())
                    + "] " + argument0;
ds_list_add(console_messages, to_be_added);
