/*
    To create a unit test do the following:
      1. write a script that takes as an argument a string that
        represents the name of the test.
      2. at the end of your test, call:
        UT_test_output(boolean result, string name, string message)
            result  -   indicates whether the test succeeded or not
            name    -   the name of the test
            message -   message that describes the result of the test
      3. add your test to the ds_map in the create event of UT_TEST
*/

show_debug_message("Why are you executing me???");
